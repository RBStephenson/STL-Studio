fastapi==0.141.1
uvicorn[standard]==0.52.4
sqlalchemy==2.0.52
alembic==1.19.1
pydantic==2.13.4
pydantic-settings==2.15.0
httpx==0.28.1
truststore==0.10.4
beautifulsoup4==4.15.0
python-multipart==0.0.32
nh3==0.3.6  # HTML sanitization for imported guide content (#440)
cryptography==50.0.0  # Fernet encryption-at-rest for the AI API key (#517)

# Painting module (#182). Color math + image handling are used from M1;
# playwright's browser (Chromium) install is deferred to M3 — the pip
# package does NOT bundle it.
numpy==2.5.2
scikit-image==0.26.0
pillow==12.3.0
playwright==1.62.0
anthropic==0.121.0
pypdf==6.16.1  # merge per-guide PDFs into a series bundle (#490)
pymupdf==1.28.2  # Paint Shelf swatch-chart import (STUDIO-329) — AGPL-3.0, see NOTICE
