name: Build Check

# Verifies the standalone executables still build on Windows and Linux.
# Runs on PRs to main so packaging breakage is caught before it reaches main.
# Main pushes are handled by main-build.yml, which publishes the rolling build.
on:
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build-check:
    permissions:
      contents: read   # artifacts only, no Release publishing
    uses: ./.github/workflows/build.yml
    with:
      release: false
